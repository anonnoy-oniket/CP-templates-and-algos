map<int, int> spf(int n) {
    map<int, int> cnt;
    for (int i = 2; i * i <= n; i++) {
        if (n % i == 0) {
            while (n % i == 0) {
                cnt[i]++;
                n /= i;
            }
        }
    }

    if (n > 1) cnt[n]++;

    return cnt;
}
